{
  "name": "easyhook-docs",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run check:links && npm run check:i18n && vite build",
    "check:links": "node scripts/check-links.mjs",
    "check:i18n": "node scripts/check-localization.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "dompurify": "^3.4.14",
    "lucide": "^0.468.0",
    "marked": "^15.0.7"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
